class Task {
  int id;
  String title;
  bool isCompleted;
  String time;
  int categoryId;

  Task({
    required this.id,
    required this.title,
    required this.isCompleted,
    required this.time,
    required this.categoryId,
  });
}
